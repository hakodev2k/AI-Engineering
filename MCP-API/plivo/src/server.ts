import {McpServer} from '@modelcontextprotocol/sdk/server/mcp.js';import {StdioServerTransport} from '@modelcontextprotocol/sdk/server/stdio.js';import {PlivoClient} from './client.js';import {tools} from './tools.js';const server=new McpServer({name:'daily-plivo-connector',version:'1.0.0'});const client=new PlivoClient();for(const t of tools)server.tool(t.name,t.purpose,(t.schema as any).shape,async(a:any)=>{try{return{content:[{type:'text',text:JSON.stringify(await t.run(a,client))}]}}catch(e:any){return{isError:true,content:[{type:'text',text:JSON.stringify({error:e.code||'ERROR',message:e.message,status:e.status,retryAfter:e.retryAfter})}]}}});await server.connect(new StdioServerTransport());
