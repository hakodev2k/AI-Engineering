import{PermitAuth}from'./auth.js';export class PermitError extends Error{constructor(public status:number,message:string,public retryAfter?:number){super(message)}}export class PermitClient{constructor(private auth=new PermitAuth(),private f:typeof fetch=fetch,private env=process.env){}base(){const r=this.env.PERMIT_REGION??'us';if(!['us','eu'].includes(r))throw new Error('PERMIT_REGION_INVALID');return r==='eu'?'https://api.eu.permit.io':'https://api.permit.io'}scope(){const p=this.env.PERMIT_PROJECT_ID?.trim(),e=this.env.PERMIT_ENVIRONMENT_ID?.trim();if(!p||!e)throw new Error('PERMIT_SCOPE_MISSING');return{p,e}}async request(path:string,init:RequestInit={},retry=true){if(!path.startsWith('/v2/'))throw new Error('PERMIT_PATH_INVALID');const max=Math.min(5,Math.max(0,Number(this.env.PERMIT_MAX_RETRIES??3)));for(let a=0;;a++){const c=new AbortController(),t=setTimeout(()=>c.abort(),Number(this.env.PERMIT_TIMEOUT_MS??10000));try{const r=await this.f(this.base()+path,{...init,signal:c.signal,headers:{Authorization:this.auth.header(),Accept:'application/json',...(init.body?{'Content-Type':'application/json'}:{}),...(init.headers??{})}});const tx=await r.text();let d:any={};try{d=tx?JSON.parse(tx):{}}catch{d={message:tx}}if(r.ok)return d;const ra=Number(r.headers.get('retry-after')??0);const e=new PermitError(r.status,String(d.detail??d.message??'Permit request failed'),ra||undefined);if(!retry||![429,500,502,503,504].includes(r.status)||a>=max)throw e;await new Promise(x=>setTimeout(x,Math.min(5000,ra?ra*1000:250*2**a)))}catch(e){if(e instanceof PermitError)throw e;if(!retry||a>=max)throw e;await new Promise(x=>setTimeout(x,Math.min(5000,250*2**a)))}finally{clearTimeout(t)}}}async get(path:string,q:Record<string,string|number|undefined>={}){const s=new URLSearchParams();for(const[k,v]of Object.entries(q))if(v!==undefined)s.set(k,String(v));return this.request(path+(s.size?'?'+s:''))}async post(path:string,body:unknown){return this.request(path,{method:'POST',body:JSON.stringify(body)},false)}}
