# PDF.co API key; kept inside the connector/upstream MCP process.
PDFCO_API_KEY=
# Optional: uvx executable path/name.
PDFCO_UVX_COMMAND=uvx
# Write-like document transformations require explicit approval by default.
PDFCO_REQUIRE_WRITE_APPROVAL=true
