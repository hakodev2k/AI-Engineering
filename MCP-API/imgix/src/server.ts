import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";import { loadConfig } from "./config.js";import { ImgixClient } from "./client.js";import { tools } from "./tools.js";
const c=loadConfig();const server=new McpServer({name:"imgix-safe-connector",version:"1.0.0"});for(const t of tools(new ImgixClient(c),c)){server.tool(t.name,t.description,t.schema.shape,async a=>{try{return{content:[{type:"text",text:JSON.stringify(await t.run(a))}]}}catch(e:any){return{isError:true,content:[{type:"text",text:e?.message??"Connector error"}]}}})}await server.connect(new StdioServerTransport());
