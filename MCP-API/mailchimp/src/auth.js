export function loadConfig(env=process.env){const token=env.MAILCHIMP_API_TOKEN?.trim();const server=env.MAILCHIMP_SERVER_PREFIX?.trim();if(!token)throw new Error('MAILCHIMP_API_TOKEN is required');if(!/^[a-z0-9-]+$/i.test(server||''))throw new Error('MAILCHIMP_SERVER_PREFIX is invalid');return{token,server,timeoutMs:Number(env.MAILCHIMP_TIMEOUT_MS||15000),approvalMode:env.MAILCHIMP_APPROVAL_MODE||'write'};}