import { z } from 'zod';
const schema=z.object({GAINSIGHT_BASE_URL:z.string().url().refine(v=>v.startsWith('https://'),'HTTPS required'),GAINSIGHT_ACCESS_KEY:z.string().min(10),GAINSIGHT_TIMEOUT_MS:z.coerce.number().int().min(1000).max(120000).default(15000),GAINSIGHT_MAX_RETRIES:z.coerce.number().int().min(0).max(5).default(2),GAINSIGHT_ALLOW_WRITES:z.enum(['true','false']).default('false'),GAINSIGHT_APPROVAL_SECRET:z.string().min(16).optional()});
export type Config={baseUrl:string;accessKey:string;timeoutMs:number;maxRetries:number;allowWrites:boolean;approvalSecret?:string};
export function loadConfig(env:NodeJS.ProcessEnv=process.env):Config{const c=schema.parse(env);const u=new URL(c.GAINSIGHT_BASE_URL);if(u.username||u.password||u.pathname!=='/'||u.search||u.hash)throw new Error('GAINSIGHT_BASE_URL must be an HTTPS origin');return{baseUrl:u.origin,accessKey:c.GAINSIGHT_ACCESS_KEY,timeoutMs:c.GAINSIGHT_TIMEOUT_MS,maxRetries:c.GAINSIGHT_MAX_RETRIES,allowWrites:c.GAINSIGHT_ALLOW_WRITES==='true',approvalSecret:c.GAINSIGHT_APPROVAL_SECRET};}
