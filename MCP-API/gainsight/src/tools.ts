import { z } from 'zod';import type { Config } from './config.js';import { GainsightClient } from './client.js';import { enforce,type Risk } from './policy.js';
const id=z.string().regex(/^[A-Za-z0-9_]{1,100}$/);const fields=z.array(id).min(1).max(50);const records=z.array(z.record(z.string(),z.union([z.string(),z.number(),z.boolean(),z.null()]))).min(1).max(50);const query=z.object({select:fields,where:z.unknown().optional(),orderBy:z.record(id,z.enum(['asc','desc'])).optional(),limit:z.number().int().min(1).max(5000).default(100),offset:z.number().int().min(0).max(1000000).default(0)}).strict();
const defs=[['gainsight.company.describe','READ',z.object({includeChildren:z.boolean().default(true)}).strict()],['gainsight.company.query','READ',query],['gainsight.company.create','WRITE',z.object({records}).strict()],['gainsight.company.update','WRITE',z.object({keys:z.array(id).min(1).max(3),records}).strict()],['gainsight.relationship.query','READ',query],['gainsight.relationship.create','WRITE',z.object({records}).strict()],['gainsight.relationship.update','WRITE',z.object({keys:z.array(id).min(1).max(3),records}).strict()],['gainsight.relationship.upsert','WRITE',z.object({keys:z.array(id).min(1).max(3),records}).strict()]] as const;
export const toolNames=defs.map(d=>d[0]);export function registerTools(server:any,client:GainsightClient,cfg:Config){for(const [name,risk,schema] of defs)server.tool(name,`Gainsight ${risk} operation (${risk}); writes require host approval.`,{input:z.any()},async({input,approval}:{input:unknown;approval?:string})=>{const parsed=(schema as any).parse(input);enforce(cfg,risk as Risk,name,parsed,approval);let method='POST',path='';if(name==='gainsight.company.describe'){method='GET';path='/v1/meta/services/objects/Company/describe?ic='+String(parsed.includeChildren)+'&cl=3&idd=true'}else if(name==='gainsight.company.query')path='/v1/data/objects/query/Company';else if(name==='gainsight.company.create')path='/v1/data/objects/Company';else if(name==='gainsight.company.update'){method='PUT';path='/v1/data/objects/Company?keys='+parsed.keys.join(',')}else if(name==='gainsight.relationship.query')path='/v1/data/objects/query/relationship';else if(name==='gainsight.relationship.create')path='/v1/data/objects/relationship';else if(name==='gainsight.relationship.update'){method='PUT';path='/v1/data/objects/relationship?keys='+parsed.keys.join(',')}else{method='PUT';path='/v1/data/objects/relationship/upsert?keys='+parsed.keys.join(',')}const body=method==='GET'?undefined:(name.endsWith('.query')?parsed:{records:parsed.records});const out=await client.request(method,path,body,risk==='READ');return{content:[{type:'text',text:JSON.stringify(out)}]};});}}
