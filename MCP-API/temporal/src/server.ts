import {McpServer} from '@modelcontextprotocol/sdk/server/mcp.js';import {StdioServerTransport} from '@modelcontextprotocol/sdk/server/stdio.js';import {specs,invoke} from './tools.js';
const s=new McpServer({name:'temporal-connector',version:'1.0.0'});for(const d of specs){s.tool(d[0],`Temporal official SDK tool; risk=${d[1]}`,d[2].shape,async a=>{try{return{content:[{type:'text',text:JSON.stringify({untrusted_provider_data:true,data:await invoke(d[0],a)})}]}}catch(e){return{isError:true,content:[{type:'text',text:JSON.stringify({error:e instanceof Error?e.message:String(e)})}]}}})}await s.connect(new StdioServerTransport());
