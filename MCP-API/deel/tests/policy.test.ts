import {describe,it,expect} from 'vitest';import {POLICY,authorize} from '../src/policy.js';
describe('Deel connector policy',()=>{it('registers a curated 10-tool allowlist',()=>{expect(Object.keys(POLICY)).toHaveLength(10);expect(new Set(Object.values(POLICY).map(x=>x.upstream)).size).toBe(10)});it('allows reads without approval',()=>expect(()=>authorize('READ',false,{})).not.toThrow());it('denies writes by default',()=>expect(()=>authorize('WRITE',true,{})).toThrow(/disabled/));it('requires approval for writes',()=>expect(()=>authorize('WRITE',false,{DEEL_ALLOW_WRITE:'true'})).toThrow(/approval/));it('allows approved write only when enabled',()=>expect(()=>authorize('WRITE',true,{DEEL_ALLOW_WRITE:'true'})).not.toThrow());it('keeps destructive operations separately gated',()=>expect(()=>authorize('DESTRUCTIVE',true,{DEEL_ALLOW_WRITE:'true'})).toThrow(/Destructive/));});