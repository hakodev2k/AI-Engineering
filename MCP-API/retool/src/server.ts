import {McpServer} from "@modelcontextprotocol/sdk/server/mcp.js";import {StdioServerTransport} from "@modelcontextprotocol/sdk/server/stdio.js";import {execute,tools} from "./tools.js";
const server=new McpServer({name:"retool-mcp-connector",version:"1.0.0"});for(const t of tools)server.tool(t.name,`${t.purpose} Permission: ${t.risk}. Approval required: ${t.approval}.`,(t.schema as any).shape,async(args:any)=>{try{const data=await execute(t.name,args);return{content:[{type:"text",text:JSON.stringify({provider:"Retool",untrustedData:true,data})}]}}catch(error){return{isError:true,content:[{type:"text",text:error instanceof Error?error.message:"Unknown error"}]}}});await server.connect(new StdioServerTransport());
