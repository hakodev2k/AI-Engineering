# Webflow OAuth access token, site token, or workspace token with least-privilege scopes.
WEBFLOW_ACCESS_TOKEN=

# Connector policy. Default is read-only.
WEBFLOW_PERMISSIONS=read
WEBFLOW_REQUIRE_WRITE_APPROVAL=true
WEBFLOW_ALLOW_DESTRUCTIVE=false
WEBFLOW_TIMEOUT_MS=15000
WEBFLOW_MAX_RETRIES=2
