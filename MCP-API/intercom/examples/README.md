# Examples

- `tool-calls.md` shows scoped MCP tool inputs, risks, and approvals.
- `mcp-client.json` shows a local stdio client configuration shape with placeholder secret-manager injection.
- `approval.env.example` shows temporary external approval configuration without credentials.
