# Source layout

- `config.ts`: environment validation and approval policy.
- `client.ts`: authenticated Intercom REST transport with timeout and bounded read retries.
- `server.ts`: MCP tool registration and strict schemas.
- `index.ts`: reusable exports for custom agent hosts.
