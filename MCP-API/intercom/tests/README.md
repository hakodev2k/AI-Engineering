# Tests

`connector.test.ts` is credential-free and uses mocked fetch responses. It covers configuration validation, approval denial/allowance, auth headers, no write retries, bounded read throttling retries, and the registered MCP tool surface.
