# Prefer OAuth2 bearer tokens with only the scopes needed by enabled tools.
FACTORIAL_TOKEN=
# API keys are supported by Factorial but are broad; use only for controlled service integrations.
FACTORIAL_API_KEY=
FACTORIAL_API_VERSION=2026-07-01
FACTORIAL_TIMEOUT_MS=15000
FACTORIAL_REQUIRE_WRITE_APPROVAL=true