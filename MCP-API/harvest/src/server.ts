import{McpServer}from'@modelcontextprotocol/sdk/server/mcp.js';import{StdioServerTransport}from'@modelcontextprotocol/sdk/server/stdio.js';import{HarvestClient}from'./client.js';import{tools}from'./tools.js';
const server=new McpServer({name:'harvest',version:'1.0.0'}),client=new HarvestClient();for(const t of tools)server.tool(t.name,t.description,(t.schema as any).shape,async(args:any)=>{try{const parsed=t.schema.parse(args),data=await t.run(parsed,client);return{content:[{type:'text',text:JSON.stringify({data,untrusted_provider_content:true})}]}}catch(e:any){return{isError:true,content:[{type:'text',text:JSON.stringify({error:e.code||'VALIDATION_OR_PROVIDER_ERROR',message:e.message,retry_after:e.retryAfter})}]}}});await server.connect(new StdioServerTransport());