export function loadConfig(env=process.env){const apiKey=env.STATUSPAGE_API_KEY?.trim();const pageId=env.STATUSPAGE_PAGE_ID?.trim();if(!apiKey)throw new Error('STATUSPAGE_API_KEY is required');if(!pageId||!/^[A-Za-z0-9_-]+$/.test(pageId))throw new Error('Valid STATUSPAGE_PAGE_ID is required');return{apiKey,pageId,allowWrites:env.STATUSPAGE_ALLOW_WRITES==='true',timeoutMs:Number(env.STATUSPAGE_TIMEOUT_MS||10000)}}
