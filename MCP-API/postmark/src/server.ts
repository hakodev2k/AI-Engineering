import {McpServer} from "@modelcontextprotocol/sdk/server/mcp.js";import {StdioServerTransport} from "@modelcontextprotocol/sdk/server/stdio.js";import {tools} from "./tools.js";
export function buildServer(){const s=new McpServer({name:"postmark-connector",version:"1.0.0"});for(const t of tools())s.tool(t.name,t.description,t.schema.shape,async raw=>{try{const input=t.schema.parse(raw);const data=await t.run(input);return{content:[{type:"text",text:JSON.stringify({ok:true,risk:t.risk,untrustedProviderData:data})}]}}catch(e){return{isError:true,content:[{type:"text",text:JSON.stringify({ok:false,error:e instanceof Error?e.message:"UNKNOWN"})}]}}});return s;}if(process.env.NODE_ENV!=="test"){const s=buildServer();await s.connect(new StdioServerTransport());}
