# Use exactly one credential type. OAuth is recommended for multi-user integrations.
LOKALISE_OAUTH_TOKEN=
LOKALISE_API_TOKEN=

# Connector policy. Default is read-only.
LOKALISE_PERMISSIONS=read
LOKALISE_REQUIRE_WRITE_APPROVAL=true
LOKALISE_TIMEOUT_MS=15000
LOKALISE_MAX_RETRIES=2
