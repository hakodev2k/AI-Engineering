import {McpServer} from "@modelcontextprotocol/sdk/server/mcp.js";import {StdioServerTransport} from "@modelcontextprotocol/sdk/server/stdio.js";import {defs,invoke} from "./tools.js";const server=new McpServer({name:"split-connector",version:"1.0.0"});for(const d of defs){server.tool(d[0],`Split REST tool; risk=${d[1]}`,d[2].shape,async a=>({content:[{type:"text",text:JSON.stringify(await invoke(d[0],a))}]}))}await server.connect(new StdioServerTransport());
