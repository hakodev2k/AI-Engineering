import test from"node:test";import assert from"node:assert/strict";import{ResendAuth}from"../src/auth.js";import{ResendClient,ResendError}from"../src/client.js";import{requireApproval}from"../src/policy.js";import{tools}from"../src/tools.js";
test("auth",()=>assert.throws(()=>new ResendAuth({}as any).token(),/AUTH_MISSING/));test("tool registration",()=>{const x=tools({}as any);assert.equal(x.length,14);assert(x.every(t=>t.name.startsWith("resend.")))});test("approval",()=>assert.throws(()=>requireApproval("WRITE",false,{RESEND_WRITE_APPROVAL:"required"}as any),/APPROVAL_REQUIRED/));test("strict schema",()=>assert.equal(tools({}as any)[0].schema.safeParse({limit:10,extra:true}).success,false));test("pagination",async()=>{const c=new ResendClient(new ResendAuth({RESEND_API_KEY:"x"}as any),fetch,{RESEND_API_BASE_URL:"https://api.resend.com"}as any);await assert.rejects(()=>c.list("/emails",101),/PAGINATION_INVALID/)});test("error mapping",async()=>{const f=async()=>new Response(JSON.stringify({name:"validation_error",message:"bad"}),{status:422});const c=new ResendClient(new ResendAuth({RESEND_API_KEY:"x"}as any),f as any,{RESEND_API_BASE_URL:"https://api.resend.com",RESEND_MAX_RETRIES:"0"}as any);await assert.rejects(()=>c.request("/emails"),e=>e instanceof ResendError&&e.status===422)});test("rate limit retry",async()=>{let n=0;const f=async()=>++n===1?new Response(JSON.stringify({message:"slow"}),{status:429}):new Response(JSON.stringify({data:[]}),{status:200});const c=new ResendClient(new ResendAuth({RESEND_API_KEY:"x"}as any),f as any,{RESEND_API_BASE_URL:"https://api.resend.com",RESEND_MAX_RETRIES:"1"}as any);await c.request("/emails");assert.equal(n,2)});test("write no blind retry",async()=>{let n=0;const f=async()=>{n++;return new Response(JSON.stringify({message:"slow"}),{status:429})};const c=new ResendClient(new ResendAuth({RESEND_API_KEY:"x"}as any),f as any,{RESEND_API_BASE_URL:"https://api.resend.com",RESEND_MAX_RETRIES:"3"}as any);await assert.rejects(()=>c.request("/emails",{method:"POST"},false));assert.equal(n,1)});
