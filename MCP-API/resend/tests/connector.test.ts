import {describe,it,expect,beforeEach} from 'vitest'; import {tools} from '../src/tools.js'; import {ApprovalError,requireApproval} from '../src/security.js';
describe('connector',()=>{beforeEach(()=>{process.env.RESEND_REQUIRE_WRITE_APPROVAL='true'});it('registers unique tools',()=>expect(new Set(tools.map(x=>x.name)).size).toBe(tools.length));it('covers useful capabilities',()=>expect(tools.length).toBeGreaterThanOrEqual(8));it('requires approval for writes',()=>expect(()=>requireApproval('WRITE',false)).toThrow(ApprovalError));it('allows reads',()=>expect(()=>requireApproval('READ',false)).not.toThrow());it('validates email send',()=>{const t=tools.find(x=>x.name==='resend.email.send')!;expect(t.schema.safeParse({from:'a@b.com',to:['bad'],subject:'x',text:'y'}).success).toBe(false)});it('destructive schema requires true approval',()=>{const t=tools.find(x=>x.name==='resend.contact.delete')!;expect(t.schema.safeParse({audienceId:'a',contactId:'b',approved:false}).success).toBe(false)})});
