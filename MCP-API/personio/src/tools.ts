import {z} from 'zod';import {PersonioClient,requireApproval} from './client.js';
export const schemas={listEmployees:z.object({limit:z.number().int().min(1).max(200).default(100),offset:z.number().int().min(0).default(0)}),getEmployee:z.object({id:z.number().int().positive()}),createEmployee:z.object({first_name:z.string().min(1).max(200),last_name:z.string().min(1).max(200),email:z.string().email(),approved:z.literal(true)}),updateEmployee:z.object({id:z.number().int().positive(),fields:z.object({first_name:z.string().min(1).max(200).optional(),last_name:z.string().min(1).max(200).optional(),position:z.string().max(200).optional(),weekly_hours:z.number().min(0).max(168).optional()}).refine(x=>Object.keys(x).length>0),approved:z.literal(true)}),listTimeOff:z.object({start_date:z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(),end_date:z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(),limit:z.number().int().min(1).max(200).default(100),offset:z.number().int().min(0).default(0)}),createTimeOff:z.object({employee_id:z.number().int().positive(),time_off_type_id:z.number().int().positive(),start_date:z.string().regex(/^\d{4}-\d{2}-\d{2}$/),end_date:z.string().regex(/^\d{4}-\d{2}-\d{2}$/),half_day_start:z.boolean().default(false),half_day_end:z.boolean().default(false),comment:z.string().max(1000).optional(),skip_approval:z.boolean().default(false),approved:z.literal(true)}),listAttendance:z.object({start_date:z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(),end_date:z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(),limit:z.number().int().min(1).max(200).default(100),offset:z.number().int().min(0).default(0)}),getWebhook:z.object({id:z.string().uuid()})};
const qs=(o:Record<string,unknown>)=>{const p=new URLSearchParams();for(const[k,v]of Object.entries(o))if(v!==undefined)p.set(k,String(v));return p.toString()};
export function handlers(c:PersonioClient){return{async employeeList(a:any){const x=schemas.listEmployees.parse(a);return c.get(`/v1/company/employees?${qs(x)}`)},async employeeGet(a:any){const x=schemas.getEmployee.parse(a);return c.get(`/v1/company/employees/${x.id}`)},async employeeCreate(a:any){const x=schemas.createEmployee.parse(a);requireApproval(c.cfg,x.approved);return c.post('/v1/company/employees',{employee:{first_name:x.first_name,last_name:x.last_name,email:x.email}})},async employeeUpdate(a:any){const x=schemas.updateEmployee.parse(a);requireApproval(c.cfg,x.approved);return c.patch(`/v1/company/employees/${x.id}`,{employee:x.fields})},async timeOffList(a:any){const x=schemas.listTimeOff.parse(a);return c.get(`/v1/company/time-offs?${qs(x)}`)},async timeOffCreate(a:any){const x=schemas.createTimeOff.parse(a);requireApproval(c.cfg,x.approved);const{approved,...body}=x;return c.post('/v1/company/time-offs',body,true)},async attendanceList(a:any){const x=schemas.listAttendance.parse(a);return c.get(`/v1/company/attendances?${qs(x)}`)},async webhookList(){return c.get('/v2/webhooks')},async webhookGet(a:any){const x=schemas.getWebhook.parse(a);return c.get(`/v2/webhooks/${encodeURIComponent(x.id)}`)}}}
