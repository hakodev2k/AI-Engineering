import {McpServer} from '@modelcontextprotocol/sdk/server/mcp.js';import {StdioServerTransport} from '@modelcontextprotocol/sdk/server/stdio.js';import {loadConfig,PersonioClient} from './client.js';import {handlers,schemas} from './tools.js';
export function buildServer(client=new PersonioClient(loadConfig())){const s=new McpServer({name:'personio',version:'1.0.0'}),h=handlers(client),reg=(name:string,desc:string,schema:any,fn:any)=>s.tool(name,desc,schema.shape,async(a:any)=>({content:[{type:'text',text:JSON.stringify(await fn(a))}]}));reg('personio.employee.list','READ: list employees; only Personio-whitelisted attributes are returned.',schemas.listEmployees,h.employeeList);reg('personio.employee.get','READ: retrieve one employee.',schemas.getEmployee,h.employeeGet);reg('personio.employee.create','WRITE: create employee; explicit human approval required.',schemas.createEmployee,h.employeeCreate);reg('personio.employee.update','WRITE: update a constrained employee field set; explicit human approval required.',schemas.updateEmployee,h.employeeUpdate);reg('personio.time_off.list','READ: list day-based time off periods.',schemas.listTimeOff,h.timeOffList);reg('personio.time_off.create','WRITE: create day-based time off; approval required; provider approval flow remains enabled by default.',schemas.createTimeOff,h.timeOffCreate);reg('personio.attendance.list','READ: list attendance periods.',schemas.listAttendance,h.attendanceList);s.tool('personio.webhook.list','READ: list configured Personio v2 webhooks.',{},async()=>({content:[{type:'text',text:JSON.stringify(await h.webhookList())}]}));reg('personio.webhook.get','READ: retrieve one Personio v2 webhook.',schemas.getWebhook,h.webhookGet);return s}
if(process.env.NODE_ENV!=='test'){const s=buildServer();await s.connect(new StdioServerTransport())}
