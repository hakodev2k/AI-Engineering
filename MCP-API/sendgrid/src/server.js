import {McpServer} from '@modelcontextprotocol/sdk/server/mcp.js';import {StdioServerTransport} from '@modelcontextprotocol/sdk/server/stdio.js';import {SendGridClient} from './client.js';import {tools,invoke} from './tools.js';
const server=new McpServer({name:'sendgrid-connector',version:'1.0.0'}),client=new SendGridClient();for(const [name,t] of Object.entries(tools))server.tool(name,`Twilio SendGrid ${t.risk} operation`,t.schema.shape,async args=>{const result=await invoke(name,args,client);return {content:[{type:'text',text:JSON.stringify(result)}]};});await server.connect(new StdioServerTransport());
