import {describe,it,expect,beforeEach} from 'vitest';import {authorize,PolicyError} from '../src/policy.js';import {schemas} from '../src/tools.js';
describe('daytona connector safety',()=>{beforeEach(()=>{process.env.DAYTONA_ALLOWED_RISKS='READ,WRITE,HIGH_RISK,DESTRUCTIVE';process.env.DAYTONA_REQUIRE_WRITE_APPROVAL='true'});it('validates ids',()=>expect(()=>schemas.id.parse({sandbox:''})).toThrow());it('rejects extra input',()=>expect(()=>schemas.id.parse({sandbox:'x',token:'secret'})).toThrow());it('allows reads',()=>expect(()=>authorize('READ')).not.toThrow());it('requires write approval',()=>expect(()=>authorize('WRITE')).toThrow(PolicyError));it('requires high-risk approval',()=>expect(()=>authorize('HIGH_RISK')).toThrow(PolicyError));it('requires destructive approval',()=>expect(()=>authorize('DESTRUCTIVE')).toThrow(PolicyError));it('allows approved destructive when enabled',()=>expect(()=>authorize('DESTRUCTIVE',true)).not.toThrow());it('bounds execution timeout',()=>expect(()=>schemas.exec.parse({sandbox:'x',command:'ls',timeoutSeconds:999,approved:true})).toThrow());});
