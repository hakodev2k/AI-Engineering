import{Client}from'@modelcontextprotocol/sdk/client/index.js';import{StreamableHTTPClientTransport}from'@modelcontextprotocol/sdk/client/streamableHttp.js';import type{Config}from'./config.js';
export class DovetailMcp{constructor(private c:Config){}async search(query:string){const client=new Client({name:'dovetail-connector',version:'1.0.0'});const transport=new StreamableHTTPClientTransport(new URL(this.c.mcpUrl),{requestInit:{headers:{Authorization:'Bearer '+this.c.token}}});try{await client.connect(transport);const listed=await client.listTools();if(!listed.tools.some(t=>t.name==='search_workspace'))throw new Error('Official MCP search_workspace tool unavailable');return await client.callTool({name:'search_workspace',arguments:{query}})}finally{await client.close().catch(()=>{})}}}
