# Optional for public dependency scoring; required for organization-scoped tools.
SOCKET_API_TOKEN=
# Override only with a trusted, pinned local Socket MCP executable if desired.
SOCKET_UPSTREAM_COMMAND=npx
SOCKET_UPSTREAM_TIMEOUT_MS=30000
