import {describe,it,expect} from 'vitest';import {loadConfig} from '../src/config.js';import {authorize} from '../src/permissions.js';import {userCreate,userList} from '../src/schemas.js';
const base={baseUrl:'https://example.zitadel.cloud',pat:'secret',timeoutMs:1000,requireWriteApproval:true,allowDestructive:false};
describe('config',()=>{it('accepts PAT',()=>expect(loadConfig({ZITADEL_BASE_URL:'https://example.zitadel.cloud',ZITADEL_PAT:'x'} as any).pat).toBe('x'));it('rejects missing credentials',()=>expect(()=>loadConfig({ZITADEL_BASE_URL:'https://example.zitadel.cloud'} as any)).toThrow())});
describe('validation',()=>{it('caps SCIM page size',()=>expect(()=>userList.parse({orgId:'o',count:101})).toThrow());it('requires approval for create',()=>expect(()=>userCreate.parse({orgId:'o',userName:'u',givenName:'A',familyName:'B',email:'a@b.com'})).toThrow())});
describe('permissions',()=>{it('allows reads',()=>expect(()=>authorize(base,'READ')).not.toThrow());it('blocks unapproved writes',()=>expect(()=>authorize(base,'WRITE')).toThrow('HUMAN_APPROVAL_REQUIRED'));it('blocks destructive even approved by default',()=>expect(()=>authorize(base,'DESTRUCTIVE',true)).toThrow('DESTRUCTIVE_DISABLED'));it('allows destructive only when enabled and approved',()=>expect(()=>authorize({...base,allowDestructive:true},'DESTRUCTIVE',true)).not.toThrow())});