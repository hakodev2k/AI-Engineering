import { z } from 'zod';
const envSchema=z.object({ZITADEL_BASE_URL:z.string().url(),ZITADEL_PAT:z.string().min(1).optional(),ZITADEL_CLIENT_ID:z.string().min(1).optional(),ZITADEL_CLIENT_SECRET:z.string().min(1).optional(),ZITADEL_TIMEOUT_MS:z.coerce.number().int().min(1000).max(60000).default(15000),ZITADEL_REQUIRE_WRITE_APPROVAL:z.enum(['true','false']).default('true'),ZITADEL_ALLOW_DESTRUCTIVE:z.enum(['true','false']).default('false')}).superRefine((v,c)=>{if(!v.ZITADEL_PAT&&!(v.ZITADEL_CLIENT_ID&&v.ZITADEL_CLIENT_SECRET))c.addIssue({code:'custom',message:'Configure ZITADEL_PAT or both ZITADEL_CLIENT_ID and ZITADEL_CLIENT_SECRET'});});
export type Config={baseUrl:string;pat?:string;clientId?:string;clientSecret?:string;timeoutMs:number;requireWriteApproval:boolean;allowDestructive:boolean};
export function loadConfig(env:NodeJS.ProcessEnv=process.env):Config{const v=envSchema.parse(env);return{baseUrl:v.ZITADEL_BASE_URL.replace(/\/$/,''),pat:v.ZITADEL_PAT,clientId:v.ZITADEL_CLIENT_ID,clientSecret:v.ZITADEL_CLIENT_SECRET,timeoutMs:v.ZITADEL_TIMEOUT_MS,requireWriteApproval:v.ZITADEL_REQUIRE_WRITE_APPROVAL==='true',allowDestructive:v.ZITADEL_ALLOW_DESTRUCTIVE==='true'};}