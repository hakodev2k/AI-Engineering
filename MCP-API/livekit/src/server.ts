import {Server} from "@modelcontextprotocol/sdk/server/index.js"; import {StdioServerTransport} from "@modelcontextprotocol/sdk/server/stdio.js"; import {CallToolRequestSchema,ListToolsRequestSchema} from "@modelcontextprotocol/sdk/types.js"; import {ToolRouter,toolDefs} from "./tools.js";
const server=new Server({name:"livekit-connector",version:"1.0.0"},{capabilities:{tools:{}}}),router=new ToolRouter(); server.setRequestHandler(ListToolsRequestSchema,async()=>({tools:toolDefs.map(({risk,...tool})=>tool)})); server.setRequestHandler(CallToolRequestSchema,async req=>{try{const data=await router.execute(req.params.name,req.params.arguments||{});return{content:[{type:"text",text:JSON.stringify({ok:true,data,untrustedProviderContent:true})}]};}catch(error:any){return{isError:true,content:[{type:"text",text:JSON.stringify({ok:false,error:{name:error?.constructor?.name||"Error",message:error instanceof Error?error.message:"Unknown error"}})}]};}}); await server.connect(new StdioServerTransport());
