import {AccessToken,RoomServiceClient,WebhookReceiver,DataPacket_Kind} from "livekit-server-sdk"; import type {Config} from "./config.js";
export interface LiveKitOps{listRooms(names?:string[]):Promise<unknown>;createRoom(options:Record<string,unknown>):Promise<unknown>;updateRoomMetadata(room:string,metadata:string):Promise<unknown>;deleteRoom(room:string):Promise<unknown>;listParticipants(room:string):Promise<unknown>;getParticipant(room:string,identity:string):Promise<unknown>;updateParticipant(room:string,identity:string,metadata?:string,permission?:Record<string,unknown>,name?:string,attributes?:Record<string,string>):Promise<unknown>;removeParticipant(room:string,identity:string):Promise<unknown>;mutePublishedTrack(room:string,identity:string,trackSid:string,muted:boolean):Promise<unknown>;sendData(room:string,payload:Uint8Array,kind:DataPacket_Kind,destinationIdentities?:string[],topic?:string):Promise<unknown>;}
export class LiveKitClient{readonly room:LiveKitOps;private receiver:WebhookReceiver;constructor(private config:Config){this.room=new RoomServiceClient(config.url,config.apiKey,config.apiSecret) as unknown as LiveKitOps;this.receiver=new WebhookReceiver(config.apiKey,config.apiSecret);}async issueJoinToken(i:{identity:string;room:string;ttlSeconds:number;name?:string;metadata?:string;canPublish:boolean;canSubscribe:boolean;canPublishData:boolean}){const t=new AccessToken(this.config.apiKey,this.config.apiSecret,{identity:i.identity,name:i.name,metadata:i.metadata,ttl:i.ttlSeconds});t.addGrant({roomJoin:true,room:i.room,canPublish:i.canPublish,canSubscribe:i.canSubscribe,canPublishData:i.canPublishData});return{token:await t.toJwt(),expiresInSeconds:i.ttlSeconds};}verifyWebhook(rawBody:string,authorization:string){return this.receiver.receive(rawBody,authorization);}}
export {DataPacket_Kind};
