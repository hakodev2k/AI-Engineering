import {describe,it,expect} from 'vitest';import {loadConfig} from '../src/config.js';import {ApprovalError,enforce} from '../src/policy.js';
describe('config and policy',()=>{it('rejects missing credential',()=>expect(()=>loadConfig({})).toThrow(/STRIPE_SECRET_KEY/));it('requires approved action for high risk',()=>{const c=loadConfig({STRIPE_SECRET_KEY:'sk_test_x',STRIPE_APPROVED_ACTION_IDS:'human-42'});expect(()=>enforce(c,'HIGH_RISK','wrong')).toThrow(ApprovalError);expect(()=>enforce(c,'HIGH_RISK','human-42')).not.toThrow()});it('can allow writes without approval but not high risk',()=>{const c=loadConfig({STRIPE_SECRET_KEY:'sk_test_x',STRIPE_REQUIRE_WRITE_APPROVAL:'false'});expect(()=>enforce(c,'WRITE')).not.toThrow();expect(()=>enforce(c,'HIGH_RISK')).toThrow()})});