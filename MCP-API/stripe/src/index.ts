import {McpServer} from '@modelcontextprotocol/sdk/server/mcp.js';import {StdioServerTransport} from '@modelcontextprotocol/sdk/server/stdio.js';import {loadConfig} from './config.js';import {StripeClient} from './client.js';import {registerTools} from './tools.js';
export async function main(){const cfg=loadConfig();const server=new McpServer({name:'stripe-connector',version:'1.0.0'});registerTools(server,new StripeClient(cfg),cfg);await server.connect(new StdioServerTransport())}if(import.meta.url===`file://${process.argv[1]}`)main().catch(e=>{console.error(e instanceof Error?e.message:String(e));process.exit(1)});
