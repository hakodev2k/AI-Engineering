import {setTimeout as delay} from 'node:timers/promises';
import type {Config} from './config.js';
export class MailerSendApiError extends Error{constructor(public status:number,public code:string,public retryAfter?:number){super(`${status}:${code}`)}}
export class MailerSendClient{constructor(private cfg:Config,private fetchImpl:typeof fetch=fetch){}
async request(method:string,path:string,body?:unknown,query?:Record<string,unknown>){const url=new URL(path,this.cfg.baseUrl);for(const[k,v]of Object.entries(query||{}))if(v!==undefined&&v!==null)url.searchParams.set(k,String(v));for(let attempt=0;attempt<3;attempt++){const c=new AbortController();const timer=setTimeout(()=>c.abort(),this.cfg.timeoutMs);try{const r=await this.fetchImpl(url,{method,headers:{Authorization:`Bearer ${this.cfg.token}`,'content-type':'application/json','user-agent':'ai-engineering-mailersend-connector/1.0'},body:body===undefined?undefined:JSON.stringify(body),signal:c.signal});const text=await r.text();let data:any={};try{data=text?JSON.parse(text):{}}catch{data={message:text}}if(r.ok)return data;const retryAfter=Number(r.headers.get('retry-after')||0)||undefined;const code=String(data?.message||data?.code||`http_${r.status}`);if((r.status===429||r.status>=500)&&attempt<2){await delay(retryAfter?Math.min(retryAfter*1000,10000):250*2**attempt);continue}throw new MailerSendApiError(r.status,code,retryAfter)}finally{clearTimeout(timer)}}throw new Error('request failed')}
}
