import test from 'node:test';import assert from 'node:assert/strict';import {tools} from '../src/tools.js';test('useful curated surface',()=>assert.ok(tools.length>=8&&tools.length<=20));test('provider scoped names',()=>tools.forEach(t=>assert.match(t.name,/^beehiiv\.[a-z0-9_.]+$/)));test('no destructive tool',()=>assert.equal(tools.some(t=>t.risk==='DESTRUCTIVE'),false));test('external-delivery writes are gated',()=>assert.equal(tools.find(t=>t.name==='beehiiv.webhook.create')?.risk,'HIGH_RISK'));
