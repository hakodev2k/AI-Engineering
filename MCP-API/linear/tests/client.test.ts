import {describe,it,expect,vi,afterEach} from 'vitest'; import {LinearClient} from '../src/client.js'; import {requireApproval} from '../src/policy.js';
afterEach(()=>{vi.unstubAllGlobals(); delete process.env.LINEAR_APPROVE_WRITES});
describe('Linear connector',()=>{it('rejects missing credentials',()=>expect(()=>new LinearClient('')).toThrow('LINEAR_API_KEY')); it('requires approval for writes',()=>expect(()=>requireApproval('WRITE',false)).toThrow('HUMAN_APPROVAL_REQUIRED')); it('allows reads',()=>expect(()=>requireApproval('READ')).not.toThrow()); it('maps GraphQL errors',async()=>{vi.stubGlobal('fetch',vi.fn(async()=>new Response(JSON.stringify({errors:[{message:'bad'}]}),{status:200,headers:{'content-type':'application/json'}}))); await expect(new LinearClient('x').query('{x}')).rejects.toThrow('bad')}); it('returns data',async()=>{vi.stubGlobal('fetch',vi.fn(async()=>new Response(JSON.stringify({data:{ok:true}}),{status:200,headers:{'content-type':'application/json'}}))); await expect(new LinearClient('x').query<any>('{x}')).resolves.toEqual({ok:true})});});
