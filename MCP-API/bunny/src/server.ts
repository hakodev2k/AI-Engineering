import {McpServer} from "@modelcontextprotocol/sdk/server/mcp.js";import {StdioServerTransport} from "@modelcontextprotocol/sdk/server/stdio.js";import {tools,execute} from "./tools.js";
const server=new McpServer({name:"bunny-mcp-connector",version:"1.0.0"});for(const t of tools)server.tool(t.name,t.description,(t.schema as any).shape,async(a:any)=>{try{const out=await execute(t.name,a);return{content:[{type:"text",text:JSON.stringify({provider:"bunny.net",untrustedData:true,data:out})}]}}catch(e){return{isError:true,content:[{type:"text",text:e instanceof Error?e.message:"Unknown error"}]}}});await server.connect(new StdioServerTransport());