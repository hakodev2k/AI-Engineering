export type Risk="READ"|"WRITE"|"DESTRUCTIVE";export function approval(r:Risk,ok:boolean,c:{requireWriteApproval:boolean;allowDestructive:boolean}){if(r==="READ")return;if(r==="DESTRUCTIVE"&&!c.allowDestructive)throw new Error("Destructive tools are disabled");if((r==="DESTRUCTIVE"||c.requireWriteApproval)&&!ok)throw new Error("Human approval required")}export function pathPart(v:string,label:string){if(!/^[A-Za-z0-9][A-Za-z0-9._/-]{0,255}$/.test(v)||v.includes("..")||v.startsWith("/")||v.endsWith("."))throw new Error(`Invalid ${label}`);return v.split("/").map(encodeURIComponent).join("/")}