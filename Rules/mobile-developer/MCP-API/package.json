{
  "name": "@ai-engineering/mcp-connectors",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "discord",
    "github",
    "jira",
    "linear",
    "notion",
    "slack",
    "stripe",
    "telegram"
  ],
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "check": "npm run build && npm run test"
  }
}
