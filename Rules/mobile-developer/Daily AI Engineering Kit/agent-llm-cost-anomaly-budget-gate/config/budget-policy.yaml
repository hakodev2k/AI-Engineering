version: 1
currency: USD
period: daily
soft_budget: 20.0
hard_budget: 30.0
per_request_max_cost: 1.50
per_user_daily_max_cost: 5.0
anomaly:
  lookback_points: 24
  minimum_points: 8
  z_score_threshold: 3.0
  growth_ratio_threshold: 2.5
  minimum_cost_delta: 0.50
retry:
  max_attempts: 2
approval:
  required_for_hard_budget_override: true
  required_for_model_upgrade_above_cost_multiplier: 2.0
